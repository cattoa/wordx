require 'spec_helper'
require 'wordx'

describe Wordx do
  it 'has a version number' do
    expect(Wordx::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
